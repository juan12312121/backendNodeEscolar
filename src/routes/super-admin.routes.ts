import { Router } from 'express';

const router = Router();

// Fase 8+: aqui se montan gestion de usuarios y operaciones exclusivas de super_admin
// Accesible por: super_admin

export default router;
